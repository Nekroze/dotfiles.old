{
    "apt-get": {
        "update": "apt-get update",
        "upgrade": "apt-get dist-upgrade",
        "apt-get""install": "apt-get install -y",
        "apt-get""remove": "apt-get remove",
        "clean": "apt-get autoremove && apt-get clean && apt-get autoclean"
    }
}
    
